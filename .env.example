# =========================================================================
# Digital Product Studio — variables de entorno (ejemplo)
# Copia este archivo a .env.local y rellena los valores reales.
# NUNCA commitees .env.local ni expongas estos secretos en el cliente.
# =========================================================================

# Base de datos PostgreSQL. Formato:
#   postgresql://USUARIO:CONTRASEÑA@HOST:5432/NOMBRE_DB?schema=public
# En desarrollo local apunta a tu Postgres local; en producción al del VPS.
DATABASE_URL="postgresql://usuario:contraseña@localhost:5432/digital_product_studio?schema=public"

# NOTA (BYOK): desde la incorporación de "Tu API Key" en /settings, cada usuario
# aporta su propia API key desde el navegador y el servidor NO usa esta variable
# para generar. Se deja como opcional/legacy; puedes omitirla.
# OPENAI_API_KEY=

# Modelo de chat de OpenAI a usar (cambiable sin tocar código y desde /settings).
# Debe soportar structured outputs (json_schema). Ej: gpt-4o-mini, gpt-4o, gpt-4.1-mini.
OPENAI_MODEL=gpt-4o-mini
